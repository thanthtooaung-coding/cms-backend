spring:
  application:
    name: bms-service
  datasource:
    url: jdbc:postgresql://localhost:1800/bms_db
    username: bms_user
    password: bms_password
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: create
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  cloud:
    gateway:
      routes:
        - id: openapi
          uri: http://localhost:${server.port}
          predicates:
            - Path=/v3/api-docs/**
          filters:
            - RewritePath=/v3/api-docs/(?<path>.*), /$\{path}/v3/api-docs
    consul:
      host: localhost
      port: 8500
      discovery:
        enabled: ${CONSUL_ENABLED:true}
        service-name: ${spring.application.name}
        instance-id: ${spring.application.name}-1
        health-check-path: /health
        health-check-interval: 15s
  mail:
    host: smtp.gmail.com
    port: 587
    username: celestix118@gmail.com
    password: "dfbx rnsq iykn sdvt"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8090
  error:
    whitelabel:
      enabled: false

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS

jwt:
  secret: QWcSdXNp2gTo6CyJuMxCDD18I7zx4aD0t2UmWoV+u9Y=

cors:
  allowed-origins: >
    https://celestix-frontend-uit.vercel.app,
    http://localhost:8080,
    http://localhost:4173,
    http://47.130.149.164:4173,
    http://47.130.149.164:8080

cloudinary:
  cloud:
    name: dsylx9zse
  api:
    key: 323724637428716
    secret: ObkjrGOodGjUiw2IFLjCEPJOksE
