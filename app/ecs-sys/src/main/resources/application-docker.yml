spring:
  application:
    name: ecs-service
  datasource:
    url: jdbc:mysql://${ECS_DB_HOST:mysql-ecs}:${ECS_DB_PORT:3306}/${ECS_DB_NAME:ecs_db}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true
    username: ${ECS_DB_USER:ecs_user}
    password: ${ECS_DB_PASSWORD:ecs_password}
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 300000
      max-lifetime: 1200000
      connection-test-query: SELECT 1
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  cloud:
    consul:
      host: ${ECS_CONSUL_ADDRESS:consul}
      port: 8500
      discovery:
        enabled: ${CONSUL_ENABLED:true}
        service-name: ${spring.application.name}
        instance-id: ${spring.application.name}-1
        health-check-path: /health
        health-check-interval: 15s
  mail:
    host: ${SPRING_MAIL_HOST:smtp.gmail.com}
    port: ${SPRING_MAIL_PORT:587}
    username: ${SPRING_MAIL_USERNAME:celestix118@gmail.com}
    password: ${SPRING_MAIL_PASSWORD:dfbx rnsq iykn sdvt}
    properties:
      mail:
        smtp:
          auth: ${SPRING_MAIL_PROPERTIES_MAIL_SMTP_AUTH:true}
          starttls:
            enable: ${SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLE:true}

server:
  port: ${ECS_PORT:8088}

management:
  endpoints:
    web:
      base-path: /
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS

jwt:
  secret: ${JWT_SECRET:QWcSdXNp2gTo6CyJuMxCDD18I7zx4aD0t2UmWoV+u9Y=}

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:5176,http://localhost:5177,http://localhost:5178,http://localhost:5179}

cloudinary:
  cloud:
    name: ${CLOUDINARY_CLOUD_NAME:dsylx9zse}
  api:
    key: ${CLOUDINARY_API_KEY:323724637428716}
    secret: ${CLOUDINARY_API_SECRET:ObkjrGOodGjUiw2IFLjCEPJOksE}

cms:
  base-url: ${CMS_BASE_URL:http://localhost:4001}

